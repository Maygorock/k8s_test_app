apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "k8s_test_app.fullname" . }}-test-connection"
  labels:
    {{- include "k8s_test_app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "k8s_test_app.fullname" . }}:{{ .Values.service.targetPort }}']
  restartPolicy: Never
